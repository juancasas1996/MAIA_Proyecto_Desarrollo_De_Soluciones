# Proyecto - Desarrollo de Soluciones
# Estimacion de la edad cerebral a partir del EEG del sueno (Sleep-EDF Database Expanded)
#
# Instalacion:  pip install -r requirements.txt

# --- Lectura y procesamiento de senales EEG ---
mne>=1.12.1

# --- Manipulacion de datos ---
numpy>=1.26.4
pandas>=2.1.4
xlrd>=2.0.1          # requerido por pandas para leer SC-subjects.xls (.xls antiguo)

# --- Analisis de senales ---
scipy>=1.11.0        # welch (densidad espectral) y estadisticas

# --- Modelado ---
scikit-learn>=1.4.0  # Ridge, validacion cruzada agrupada por sujeto

# --- Seguimiento de experimentos ---
mlflow>=3.15.2       # misma version que el servidor del EC2: cliente y servidor
                     # con versiones distintas dan errores dificiles de diagnosticar
PyYAML>=6.0          # Experimentos/config.py lee config.yaml

# --- Visualizacion ---
matplotlib>=3.8.0
plotly>=6.1.0
kaleido>=1.0.0       # exporta las figuras de plotly como PNG para que se vean en GitHub.
                     # kaleido 1.x requiere plotly 6.1+; con plotly 5.x hay que usar kaleido 0.2.1

# --- Entorno de ejecucion ---
jupyter>=1.0.0
ipykernel>=6.28.0

# --- Versionado de datos ---
dvc>=3.0.0
mlflow==3.15.2

